        processor 6502
        org $c123

;;; Immediate
        ;; INC: inc from zero = 1

        ;; INC: multiple increments

        ;; INC: result lesser than 0xff

        ;; INC: result greater than 0xff

        ;; INX: inc from zero = 1

        ;; INX: multiple increments

        ;; INX: result lesser than 0xff

        ;; INX: result greater than 0xff

        ;; INY: inc from zero = 1

        ;; INY: multiple increments

        ;; INY: result lesser than 0xff

        ;; INY: result greater than 0xff

        ;; DEC: inc from zero = 1

        ;; DEC: multiple increments

        ;; DEC: result lesser than 0xff

        ;; DEC: result greater than 0xff

        ;; DEX: inc from zero = 1

        ;; DEX: multiple increments

        ;; DEX: result lesser than 0xff

        ;; DEX: result greater than 0xff

        ;; DEY: inc from zero = 1

        ;; DEY: multiple increments

        ;; DEY: result lesser than 0xff

        ;; DEY: result greater than 0xff

;;; Absolute

;;; Absolute indexed x

;;; Zeropage

;;; Zeropage indexed x
